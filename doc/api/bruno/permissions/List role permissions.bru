meta {
  name: List role permissions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/roles/{{roleId}}/permissions
  body: none
  auth: inherit
}

headers {
  X-User-Roles: {{userRoles}}
}

docs {
  Lista los pares (resourceUuid, flags) asociados al rol indicado.
}
