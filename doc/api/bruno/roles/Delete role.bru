meta {
  name: Delete role
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/roles/:roleId
  body: none
  auth: inherit
}

params:path {
  roleId: {{roleId}}
}

headers {
  X-User-Roles: {{userRoles}}
}
