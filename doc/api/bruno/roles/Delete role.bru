meta {
  name: Delete role
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/auth/roles/:roleId
  body: none
  auth: none
}

params:path {
  roleId: {{roleId}}
}

headers {
  X-User-Roles: {{userRoles}}
}
