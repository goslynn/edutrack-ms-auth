meta {
  name: Get role
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/roles/:roleId
  body: none
  auth: inherit
}

params:path {
  roleId: {{roleId}}
}

headers {
  X-User-Roles: {{userRoles}}
}
