meta {
  name: Revoke role from user
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/users/{{userId}}/roles/:roleId
  body: none
  auth: inherit
}

params:path {
  roleId: {{roleId}}
}

headers {
  X-User-Roles: {{userRoles}}
}

docs {
  Revoca un rol previamente asignado a un usuario.
}
